amends "../../pkg/schemas/Config.pkl"
import "../../pkg/schemas/docker/Docker.pkl"

docker {
  images {
    ["picklr-custom"] {
      name = "picklr-custom:v1"
      buildContext = "."
      dockerfile = "Dockerfile"
    }
  }
}
