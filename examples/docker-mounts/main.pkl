amends "@pklr/Config.pkl"
import "@pklr/Docker.pkl"

docker {
  volumes {
    ["picklr-data"] {}
  }

  containers {
    ["picklr-db"] {
      image = "alpine:latest"
      command { "tail"; "-f"; "/dev/null" }
      volumes {
        "picklr-data:/data"
      }
    }
  }
}
