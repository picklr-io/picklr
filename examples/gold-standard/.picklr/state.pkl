// Picklr state file
amends "../../pkg/schemas/core/State.pkl"

version = 1
serial = 2
lineage = ""

outputs = new {}

resources {
  new {
    type = "docker_network"
    name = "gold-public-net"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_network"
    name = "gold-internal-net"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_volume"
    name = "postgres_data"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_volume"
    name = "redis_data"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_volume"
    name = "minio_data"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_volume"
    name = "prometheus_data"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_volume"
    name = "grafana_data"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-traefik"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-rest-api"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-grpc-api"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-websocket-api"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-postgres"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-redis"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-minio"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-prometheus"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
  new {
    type = "docker_container"
    name = "gold-grafana"
    provider = "docker"
    inputs = new {}
    inputsHash = ""
    outputs = new {}
  }
}
